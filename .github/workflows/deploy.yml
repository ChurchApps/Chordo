name: Deploy

on:
  push:
    branches: [production]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm install
      - run: npm run build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      # Worker first: if this fails the job stops before S3 gets a frontend pointing at a missing /api/proxy.
      - name: Deploy proxy worker
        run: npx --yes wrangler@4 deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Configure worker S3 secrets
        run: |
          printf '%s' "$CHORDO_S3_ACCESS_KEY_ID" | npx --yes wrangler@4 secret put AWS_ACCESS_KEY_ID
          printf '%s' "$CHORDO_S3_SECRET_ACCESS_KEY" | npx --yes wrangler@4 secret put AWS_SECRET_ACCESS_KEY
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CHORDO_S3_ACCESS_KEY_ID: ${{ secrets.CHORDO_S3_ACCESS_KEY_ID }}
          CHORDO_S3_SECRET_ACCESS_KEY: ${{ secrets.CHORDO_S3_SECRET_ACCESS_KEY }}

      - run: aws s3 sync dist/ s3://chordo.org --delete

      - name: Purge Cloudflare cache
        run: |
          curl -sf -X POST "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything":true}'
